## TODOs

1. Add optional base interface for all generated class types
2. add ability to pass the source files in instead of having to specify a directory
3. add option to have converter be public instead of internal
4. make the cli return an error code if failed(via recursive loop)
